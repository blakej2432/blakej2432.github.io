---
title: "[LLM] ReAct 방법론 논문 이해와 구현"
date: 2025-06-08 18:24:28 +0900
categories: llm
tags: llm ReAct agent langchain
header:
  teaser: "../../img/cover/llm_logo.png"
---

REACT: SYNERGIZING REASONING AND ACTING IN LANGUAGE MODELS<br>

🔗[REACT: SYNERGIZING REASONING AND ACTING IN LANGUAGE MODELS](https://arxiv.org/abs/2210.03629 "https://arxiv.org/abs/2210.03629")


논문을 읽고, LangChain으로 구현해보았다.

### ReAct (Reason + Act) 왜?

LLM이 입력 대한 효과적인 출력을 해내기 위한 방법론으로 기존에는 Reason과 Act를 별개로 연구해왔음

- Reasoning (**CoT**, 사고 흐름 유도, 계획, 추론) + Acting( 지식 기반이나 환경과 같은 외부 소스로부터 추가 정보를 수집, 인터페이스 Wikipedia API) → **ReAct**


![](/img/2025-06-08/0608-1.png)


### Agent가 받는 문맥

![](/img/2025-06-08/0608-2.png)

- o: 관찰, a: 행동

![](/img/2025-06-08/0608-3.png)

![](/img/2025-06-08/0608-4.png)


- Action 공간을 L(언어 공간)으로 확장:  “Thought” or “Reasoning Trace”

![](/img/2025-06-08/0608-5.png)

- => 쉽게 말하자면, **스스로 어떤 값을 도출하거나, 그로 인해 행동하는 것들을 본인에게 다시 입력하여 최종 결과를 도출 한다는 것**


-----------
<br>

### 벤치마크 데이터셋

HotPotQA, FEVER → Wikipedia 문서를 통해 추론하여 응답해야하는 과제

- **HotpotQA**: Multi-hop question, 단일 문서 내에서 답변을 찾을 수 있는게 아니라, 여러 문서나 정보 소스 간의 연관성을 추적하고 종합하여 답변을 도출해야하는 질문으로 구성
- **FEVER**: 주장이 주어지고, 소스로 부터 (참, 거짓, 알 수 없음) 중 하나를 판단해내기

### 행동 공간(**Action Space**)

- search[entity]: 해당 “entity” 위키 페이지에서 첫 5문장 반환
- lookup[string]: “string”이 포함된 다음 문장 반환 (Ctrl + F 기능)
- finish[answer]: 정답. 현재 과제 완료

### 프롬프팅

Thought(생각)-Action(행동)-Observation(관찰)

ReAct의 추론 형식 경로 예시를 만들어놓고, 무작위 샘플(HotpotQA 6개, FEVER 3개) 로 Few-shot

![](/img/2025-06-08/0608-6.png)

```text
ReAct 내의 단계는 7단계, 5단계 이상 넘어가도 성능 향상을 발견하지 못함

CoT-SC (여러개의 CoT를 설정해 답을 내어 보고, 다수결로 답 결정)
- ReAct → CoT-SC: ReAct를 한 후에 답을 반환하지 못하면, 이후 CoT-SC만 해서 답 반환
- CoT-SC → ReAct: CoT-SC 샘플 답안 중 다수가 n/2 미만일 때, ReAct 실행

ReAct
외부 지식 기반 → Fact
but 적절한 다음 행동 못찾고 루프에 빠져버리는 오류

유익하지 않은 검색하는 오류 (결국에 우리가 RAG에서 해결해야 할 문제가 아닐까..)
```

![](/img/2025-06-08/0608-7.png)
- 3,000개 예제만 학습. 62B 로도 540B 능가

![](/img/2025-06-08/0608-8.png)
- AlfWorld(텍스트 기반 게임 형식 데이터셋), WebShop(원하는 스펙에 맞는 상품을 찾는 과제) 에서 ReAct의 효과

### ReAct 활용 Agent 구현 with Langchain
```python
from langchain_community.vectorstores import Chroma
from langchain_community.document_loaders import PyPDFium2Loader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings

embedding_function = OpenAIEmbeddings()

# 데이터 로드
loader = PyPDFium2Loader("국가안전시스템 개편 종합대책 대국민 보고(12.31. 기준).pdf")
documents = loader.load()
documents

# 벡터 DB
text_splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=30)
docs = text_splitter.split_documents(documents)
docs

db = Chroma.from_documents(docs, embedding_function)

from langchain_community.vectorstores import Chroma
from langchain.agents.agent_toolkits import create_retriever_tool
from langchain.agents import initialize_agent, AgentType, Tool

chroma_retriever = db.as_retriever(search_type="mmr", search_kwargs={"k": 2})

# agent가 사용할 Tool 설정
retriever_tool = create_retriever_tool(
    chroma_retriever,
    name="doc_search",
    description="답변을 위한 정보를 담은 문서를 찾아옵니다.",
)

tools = [retriever_tool]

from langchain import hub
prompt = hub.pull("hwchase17/react-json")

from langchain_openai import ChatOpenAI

llm = ChatOpenAI(model="gpt-4o", temperature=0)

from langchain.agents import AgentExecutor, create_react_agent
from langchain.agents.output_parsers import ReActJsonSingleInputOutputParser
from langchain.tools.render import render_text_description_and_args

# ReAct 방법론 적용한 Agent
agent = create_react_agent(
    llm,
    tools,
    prompt,
    tools_renderer=render_text_description_and_args,
    output_parser=ReActJsonSingleInputOutputParser(),
)

from langchain.agents import AgentExecutor
agent_executor = AgentExecutor(agent=agent, tools=tools, verbose=True)
response = agent_executor.invoke({"input": "119 구급 스마트시스템 개발·운영을 시작한 년도의 시군구 지역안전관리위원회 개최 횟수는? 횟수만 출력해줘"})
response

```

- 이렇게 ReAct 방법론을 적용하여 llm agent를 구성하면,<br>
**생각**, **행동**, **관찰** 을 반복하며 가장 적절한 답을 스스로 찾아내려 한다

------